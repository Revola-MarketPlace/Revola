# Revola Telegram Verification Bot

This directory contains the configuration and architecture for the Telegram payment receipt verification bot.

### Configuration
1. Copy `.env.example` to `.env`
2. Set `BOT_TOKEN` from @BotFather
3. Set `BACKEND_API_URL` pointing to the Revola API
